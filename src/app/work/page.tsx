import Navigation from "../../components/Navigation"
import { motion, AnimatePresence } from "framer-motion";

export default function Work() {
    return (
        <div>
            <Navigation />
            <h1 className="font-lato p-24">Work</h1>;
        </div>
    );
}
